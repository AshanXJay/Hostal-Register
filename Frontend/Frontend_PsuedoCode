START
  DECLARE enrollment number
          destination
          current system time
  WHILE (Always_true) DO
    GET Student's enrollment number
    IF enroll_no exists in database THEN
      GET destination
      GET current system time
      ADD record to database with enrollment number, destination, and time
    ELSE
      DISPLAY "Invalid Enrollment number"
    END IF
  END WHILE
END